# Changelog

本文档格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本 SemVer](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added
- 待补：自动化抽词（从正文自动识别可编码概念）
- 待补：CI 集成（门禁进流水线）
- 待补：Drain 等成熟日志模板算法的对比实验

## [0.6.0] - 2026-09-25

### Added
- **`tools/lexicon_audit.py`** —— 字典可用性审计，机器判据 R1~R6：
  - `R1` 字典膨胀：索引行长度 ≤ `--index-budget`（默认 300），超限报 WARN 并给出
    「应并入字典文件的候选」（按 term 长度降序）
  - `R2` 代号不可读：`term` 为空 → FAIL（§5.2）；索引行出现裸 `@`/`^` 代号 → FAIL
  - `R3` 映射漂移：同 `term` 双 `code` → FAIL；同 `code` 的 `rev` 变化 → FAIL
    （调 `src/lexicon.compare_rev`）；`match` 别名跨条目重叠 → WARN
  - `R4` 替换收益：`saving = freq × (len(meaning) − len(code))`，≤ 0 → WARN 并列出代号
  - `R5` 压缩悖论：常驻成本 + 一次性展开成本 vs 基线（§10.3），超基线 → FAIL
  - `R6` 语义风险：代号应可联想（缩写 或 与 term ≥2 连续字母重合），不透明 → WARN；
    顺序编号默认 WARN，`--allow-seq` 跳过
  - `--self-test`：内嵌 16 组样例，自证 6 项判据各自的正例 / 反例都能被触发
  - 退出码：任一项 FAIL → 1，全 OK/WARN → 0；`--json` 给结构化结果
- **`tools/benchmark.py`** —— 多文件基准，机器判据 R7：
  - 逐文件算**基线**（不含换行）/ **压缩后常驻**（头部 + 替换后正文）/ **比值**，
    并用 `src/decode.expand_file` **真跑展开**证明往返完整（§9.2）
  - 单遍**最长匹配**替换（长 term 命中时不被短 term 拆开二次替换）
  - 聚合统计（文件数 / 基线合计 / 压缩后合计 / 整体比 / 完整性通过数）；
    `--max-files` 截断**如实报数**；文件不存在计为**完整性失败**（不静默当「跳过」）
  - 退出码：0 = 全部往返完整，1 = 存在完整性失败，2 = 用法/路径错误
- `examples/docs_corpus.lexicon.tsv` + `examples/docs_corpus.index.txt` ——
  以本仓 `docs/` 真实语料挖出的 14 词示例字典与配套索引行（供上两工具真跑演示）
- **测试新增 10 项**（`33` → **`43`**，全部通过、退出码 0）：
  - R1~R6 各一项（含**负向测试**：构造违规输入，断言判据**确实触发**）
  - R7 两项：多文件聚合 + 往返完整性（缺文件必须计失败）、单遍最长匹配
  - 审计工具 `--self-test` 与退出码契约（**子进程**真跑，验的是 CI 门禁契约）
  - `tools/` 新工具零第三方依赖

### 实测验证
- `python tests/run_tests.py` → **43/43 通过，退出码 0**
- `python tools/lexicon_audit.py --self-test` → **17/17 通过，退出码 0**
  （16 组样例 + 2 项退出码语义；自证过程抓到本工具自身 4 个缺陷并已修）
- `python tools/benchmark.py docs --dict examples/docs_corpus.lexicon.tsv` →
  6 文件纳入 / 基线 54,924 → 常驻 54,449 / 整体比 **0.9914** / 完整性 **6/6**

### 说明（诚实口径）
- 上条基准的比值接近 1，是**真实结论**而非失败：本仓 `docs/` 是低重复度的原创散文，
  可编码概念密度低，头部固定开销（25 字符/文件）几乎吃掉全部收益。
  这正说明 R7 的必要性 —— 单点实测会掩盖「短文本 + 低重复 = 压缩为负」。
- `R4` 的 `freq` 取自 term 与各别名的**单串出现次数最大值**（别名常互为子串，
  求和会重复计数）；它是**下界**，不含跨语言改写与模态后缀等更细口径。

## [0.5.0] - 2026-09-25

### Changed
- **项目改名**：`agent-lexicon` → `agent-codebook`
  - 原因：GitHub 上 `SkeinRank/agent-lexicon` **精确同名**（且已占用 PyPI 包名 `agent-lexicon`），检索互相干扰
  - 新名与规范 §2.3 的术语「**码本替换**（codebook substitution）」一致；GitHub / PyPI 均无占用
  - 旧地址自动重定向（GitHub 侧）

### Fixed
- **版本号三方矛盾**：`docs/05` 声明 `v0.1 草案（未实现）`，而 CHANGELOG 已到 `0.4.0`、参考实现早已落地
  - `docs/05` → `v0.5`，状态改为「参考实现见 `src/`，测试 33/33 通过」
  - `src/validate.py` 的 `CURRENT_SPEC_VERSION` → `0.5`
  - `docs/06` 来源引用同步
- **`E-3` 概念混淆**：原文把「头部 `#LX<version>` 版本号」与「规范版本」当成同一事物
  - 二者**分离**：前者是**线格式版本**（整数，当前 `1`），后者是**文档版本**（语义化，当前 `v0.5`）
  - 仅**破坏性变更**才升线格式版本

### 实测验证
- `python tests/run_tests.py` → **33/33 通过，退出码 0**

## [0.4.0] - 2026-09-25

### Added
- **规范 §2.3「载体分离与访问门控」[规范]**：`AC-1`~`AC-3` 三条前提 ·
  **不提供的性质**表（禁止宣称）· **表述规范**表
  - 应当：「**载体分离 · 需字典方可还原**」
  - 可以：「字典作为**码本**，门控依赖**持有**」
  - **禁止**：「加密」「安全加密」「防泄露」（未达密码学强度，易被拆穿）
- **README 新增「三、它带来什么（五条主张 · 逐条校准）」**：每条给实测数据与**能站住的说法**，
  与实测不符的**改述**（第 3 条走门控表述、第 4 条走「少读常驻」）
- **06 负例库新增 C4「第五轮 · 实现反哺」**（`R5-1`~`R5-11`）：写出参考实现后才暴露的缺陷

### Fixed（实现反哺 · 7 项）
- §3.1 `body` 允许终止符前换行 → **§16.2 的 `;;` 独立成行终于可解析**（与 `L-9` 一致）
- §3.5 补 `127.0.0.1:5432` 类含冒号文本的**合法写法**（须用界定字面量）
- §8.1 `LX-W004` 改为「`term` 列存在但值为空」；缺**列**报 `LX-E010`（消严重级冲突）
- §16 给出**合规头部变体**（用类别前缀须声明 `ns=R`），并写清 `ns` 义务
- 06 负例库 B-② 列数纠正（列数「多于」列名行 → `LX-E009`；「少于」→ `LX-E010`）

### Changed
- README 章节重编号（原「三、效果」→「四」，后续顺延）；新增 §2.3 后 `INV-1` 补第二重理由

### 实测验证
- `python tests/run_tests.py` → **33/33 通过，退出码 0**（参考实现 2460 行，零第三方依赖）

## [0.3.0] - 2026-09-25

### Added
- **docs/05_引索符系统规范.md**（17.6KB）：⭐ **核心规范** ——
  20 符符号表（6 组）· EBNF 形式语法 · 头部/字面量/消歧规则 ·
  字典 TSV 规范（含字段转义）· 12 错误码 + 5 警告码 · 13 条校验规则（V-12 撤销）·
  三条不变量 · 跨宿主 L0~L3 · 版本演进
- **docs/06_负例库.md**（12.4KB）：**34 项真实缺陷**整理成可对照反例，
  标注「错在哪 / 正确做法 / 关联章节」，供实现者自检、审计者构造用例
- **src/**：⭐ **参考实现**（零第三方依赖，仅标准库）
  - `lexicon.py` — 字典层（TSV 读写 / 字段转义 §5.1.1 / 列与重复校验）
  - `validate.py` — 词法·语法解析 + §8 全部校验规则（语法层/语义层分离，§8.4）
  - `decode.py` — 展开层（§9 六步 + ND-1~ND-4 禁止静默降级 + V-10 可展开性）
  - `encode.py` — 编码层（§7 八步 + §10.3 盈亏平衡判据）
- **tests/**：`run_tests.py` + `vectors/golden.json`
  - 11 条正例 + **16 条反例（逐条必须被拒绝）** + 规范自动提取 + V-10 有效性 + 负向测试

### Fixed
- §16.3 字典列名 `#code` → `code`（违反 §5.1「列名禁止任何前缀」，自家示例违规）
- §16.1/§16.2/§16.4 字数与比值改为**实测值**（原宣称 127/45/2.82:1 与实际不符）

### Changed
- §6.1 删除重复的 `code` 产生式 → 改为指向 §3.1（**唯一来源**）
- §3.3 补路径解析基准；`ns` 交叉引用 §4.2 → §6.2
- §2.1 补不变量↔校验映射；§12 明确 L2/L3 为便利层、非门槛
- §7 步7 强度与 §8.1 对齐；步8 去掉不存在的「MUST 级」；步5 口径与 §5.2 一致

### 实测验证
- `python tests/run_tests.py` → **32/32 通过，退出码 0**
- §16 最小示例：原文 118 字符 → 符号串 45 字符（不含换行），解析→校验→展开全通

## [0.2.0] - 2026-09-25

### Added
- **docs/04_三级概括法规范.md**（13.8KB）：⭐ **核心规范** ——
  跨域统一的三级模型、损失标记体系、编码规则、质量门禁、**4 个真实反例**
- **tools/** 工具层（全部真跑验证）：
  - `tier_common.py` — 共用模块（损失标记 / 保护词表 / 统一报告）
  - `tier_text.py` — 文本域三级概括
  - `tier_code.py` — 代码域三级封装（Python 走 AST，其他语言降级正则大纲）
  - `tier_log.py` — 日志域三级压缩（模板挖矿 + 无损重建校验）
  - `roundtrip_check.py` — 独立往返复核（**不复用生成侧代码路径**）
  - `lexicon_check.py` — 从 `examples/` 迁入

### Changed
- README 新增「三级概括法」上位框架说明 + 工具清单 + 实测数据表
- `examples/lexicon_check.py` → `tools/lexicon_check.py`（文档引用同步更新）

### 实测数据

| 域 | 输入 | L1 | L2 | L3 | 压缩比 | 无损性 |
|:---|:---|:---|:---|:---|:---|:---|
| 文本 | `MEMORY.md` | 17,998 | 16,183 | 16,183 | 1.1 : 1 | 要素零丢失 |
| 代码 | `tier_code.py` 自身 | 10,986 | 1,068 | 447 | **24.6 : 1** | 签名全保留 |
| 日志 | 2000 行合成日志 | 114,799 | 98,369 | 58,950 | 1.9 : 1 | **100% 可重建** |

### 真跑抓到的 bug（工具开发中）

- 🔴 `tier_log` 模板化：逐条规则串行 sub 导致哨兵被二次替换 → **6/6 行重建失败**
  → 改为单次 alternation 匹配后 **2000/2000 通过**
- 🔴 `tier_log` 未拦膨胀：小样本时 **L2 > L1（161%）** → 新增「L2 未膨胀」FAIL 门禁 + 小样本 WARN
- 🔴 `tier_text` 逻辑顺序错：先删套话会把丢弃规则所需的标识词一起删掉 → 改为**先判留/丢、再删减**
- 🟡 `tier_text` L3 命中恒为 0：字典「含义」列是说明性文字，正文并无此字面串
  → 新增**可选「匹配式」列**（支持多别名 + 正则）
- 🟡 `roundtrip_check` 自身 2 个 bug：规则顺序收字段导致错位（**342 行误报**）；
  反引号配对被打散导致要素误判（**36 项误报**）→ 均修正，现 **0 误报**
- 🟡 `lexicon_check` 路径解析按字典目录 → 真实字典 **30 个假 FAIL**
  → 改多候选根 + 别名后 **FAIL=0**

## [0.1.0] - 2026-09-25

首个草稿版本，整理自本项目 2026-09-24 的实测迁移。

### Added
- **README.md**：方案总览（问题 / 三步机制 / 效果 / 对照 / 快速上手 / 局限）
- **docs/01_设计思路.md**：三个硬约束、五条核心设计决策、与相邻方案的边界、七条设计原则
- **docs/02_调研报告.md**：应用场景（含行业硬上限证据）、15 篇论文 + 10 个项目对照、**新颖性分析（诚实版）**
- **docs/03_失效模式.md**：真实故障链、根因分析、根治修法、防复发清单、可复现验证步骤
- **tools/lexicon_check.py**：字典可达性校验脚本（可用，真跑通过）
- **examples/example_dictionary.md**：字典格式范例

### 关键结论
- 核心思想（缩写字典 / 无损可逆）**非首创**：CompactPrompt、arXiv:2604.13066 已有同源工作
- **我们的空白**：字典**外置 + 惰性加载**，且以**注入截断安全**为一等设计目标
- 现有工作**全部把字典放在 prompt 内部**（文档内 / system prompt 内）→ 字典本身仍占上下文额度

### 实测数据（本仓 2026-09-24）
- 正文内字典占用：**3,196 字符（16.1%）→ 262 字符（索引行）**
- 净腾出注入额度：**约 2.7~2.9K 字符**
- 字典独立文件：**3,599 字符 / 29 词**，不再受正文截断影响
- 可达性校验：**29 条路径全部可达**

### 修复的失效模式
- 🔴 **字典内嵌 + 尾部截断 → 代号全变天书**（静默失效，不报错不崩溃）
- 附带根治：文档自相矛盾（机制变更未同步引用点，4 处已同步）
